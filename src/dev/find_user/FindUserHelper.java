package dev.find_user;

import java.io.IOException;
import java.util.ArrayList;
import java.util.List;
import java.util.Set;

/*
* This helper class will be used to access the users in the database.
* */
public class FindUserHelper {
    Set<String> usersInDataBase;
    DatabaseReader reader = new DatabaseReader();

    // TODO get all lines and create users out of them add them to a list/set
    // TODO look for the input ID into the list/set






}

/*
* I first need to get all the lines in the file.
* I need to get the user ID of all users and make a set.
* I need to have another class where I create a User from all lines. - this should only happen when needed.
* If the system is looking for a non-existent user, we throw an error.
* */



